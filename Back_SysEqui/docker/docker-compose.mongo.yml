services:
  mongo:
    image: mongo:8
    restart: always
    container_name: sysequi-mongodb
    ports:
      - "27017:27017"
    networks:
      - mongo_network
    volumes:
      - sysequi-mongodb-data:/data/db

  # mongo-express:
  #   image: mongo-express:latest
  #   restart: always
  #   container_name: mongo-express
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     - ME_CONFIG_MONGODB_SERVER=mongo
  #     - ME_CONFIG_MONGODB_PORT=27017
  #   networks:
  #     - mongo_network
  #   depends_on:
  #     - mongo

networks:
  mongo_network:
    driver: bridge

volumes:
  sysequi-mongodb-data:
    external: true
